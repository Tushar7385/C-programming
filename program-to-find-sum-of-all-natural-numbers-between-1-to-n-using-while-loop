//Write a Program to find sum of all natural numbers between 1 to n.

#include <stdio.h>

int main()
{
    int i,n,sum;

    i=1;
        printf("enter an integer");
        scanf("%d",&n);
        while(i<=n)
       {sum=sum+i;
          printf("%d ",sum);
            i++;        }
}
