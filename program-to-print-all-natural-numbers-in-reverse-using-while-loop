//Write a Program to print all natural numbers in reverse (from n to 1).

#include <stdio.h>

int main()
{
    int i,n;

    printf("type an integer");
    scanf("%d",&n);
    
    i=n;
        while(i>=1)
        {    printf("%d",i);
            i--;        }
}
