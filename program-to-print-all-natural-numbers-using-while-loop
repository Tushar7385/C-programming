//Write a Program to print all natural numbers from 1 to n. 

#include <stdio.h>

int main()
{
    int i,n;
    i=1;
    printf("type an integer");
    scanf("%d",&n);
        while(i<=n)
        {    printf("%d",i);
            i++;
        }
}

