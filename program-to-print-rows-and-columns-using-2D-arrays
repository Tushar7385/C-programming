//rows and columns using 2D arrays
#include <stdio.h>

int main()
{
    int A[3][3],i,r,c;
    
    for(r=0;r<=2;r++)
        for(c=0;c<=2;c++)
            scanf("%d",&A[r][c]);
    for(r=0;r<=2;r++)
        {for(c=0;c<=2;c++)
            printf("%d",A[r][c]);
            printf("\n");
        }
    
    
}
